# KakuSave プライバシーポリシー

最終更新: 2026-08-30

KakuSave（以下「本拡張機能」）は、noteで執筆中の本文テキスト履歴をユーザー自身のChromeプロファイル内に保存するローカルバックアップ機能です。

## 1. 処理するデータ

本拡張機能は、機能提供のため次の情報をブラウザ内で処理します。

- note編集画面の本文テキスト
- 本文テキストの文字数・差分情報
- 記事を区別するためにnote編集画面のURLから得られる記事識別情報
- 保存時刻、スナップショット種別、ローカル履歴管理用の内部ID

タイトル、画像ファイル、埋め込みコンテンツ、noteのログイン資格情報、Cookie、パスワードを保存対象にはしません。

## 2. 外部送信

本文テキストおよび保存履歴を、開発者のサーバー、広告事業者、解析サービスその他の第三者へ送信しません。

本拡張機能のextension pageはContent Security Policyで `connect-src 'none'` を指定し、履歴画面から外部通信を行わない設計です。

## 3. 保存場所

保存データはChrome拡張機能専用originのIndexedDBに保存されます。

- 開発者側のクラウドには保存されません
- Chromeプロファイル内では本拡張機能独自の暗号化を行いません
- `unlimitedStorage` 権限を使用しますが、本拡張機能側で256MBの安全上限を設けます

端末・OS・Chromeプロファイル自体へのアクセス権を持つ第三者からの保護は、本拡張機能単体では保証できません。

## 4. 保存期間と削除

保存履歴は、ユーザーが履歴画面から削除するまで、またはChrome側で拡張機能データが削除されるまでローカルに保持されます。

一時draft等の不要データは `chrome.alarms` を利用した日次整理の対象になります。

ユーザーは次の方法でデータを削除できます。

- KakuSaveの履歴画面から対象原稿の過去履歴をリセット
- Chromeから本拡張機能を削除し、拡張機能データを削除

## 5. 分析・広告・販売

本拡張機能は、本文内容や利用履歴を広告配信、行動ターゲティング、第三者への販売に利用しません。

v0.9 Previewでは開発者向けテレメトリーや外部分析SDKも使用しません。

## 6. 権限

### `unlimitedStorage`
本文履歴をユーザーのChromeプロファイル内にローカル保存するために使用します。無制限に保存し続ける設計ではなく、自前の安全上限と履歴整理処理を設けています。

### `alarms`
Manifest V3 Service Workerの稼働時間に依存せず、不要になった一時draftデータを定期整理するために使用します。

本拡張機能は `<all_urls>` を要求せず、content scriptの対象をnote編集ルートに限定します。

## 7. noteとの関係

KakuSaveは個人開発による非公式Chrome拡張機能であり、note株式会社が提供・承認・保証する製品ではありません。

note側の仕様変更により、本拡張機能が一時的に本文エディタを検出できなくなる場合があります。

## 8. 変更

機能・保存対象・外部通信の有無など、プライバシーに影響する変更を行う場合は、本ポリシーとChrome Web Store上の開示を同時に更新します。

特に、将来クラウド同期・アカウント機能・分析機能等を追加する場合、v0.9の「外部送信なし」という説明をそのまま流用しません。
